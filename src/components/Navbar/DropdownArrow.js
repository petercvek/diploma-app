// @flow
import { Animated } from 'react-native';
import styled from '@emotion/native';

const DownArrow = styled(Animated.Image)({
  width: 13,
  height: 7,
  top: 24,
  marginLeft: 7,
});

export default DownArrow;
